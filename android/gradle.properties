# Give Gradle enough memory for the Android plugin, and keep the build quiet.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

# Only needed if you build without Android Studio; point it at your SDK.
# sdk.dir=C\:\\Users\\you\\AppData\\Local\\Android\\Sdk
